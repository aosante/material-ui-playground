import Typography from '@material-ui/core/Typography';

const Create = () => {
  return (
    <div>
      <Typography color="secondary">Create New Note</Typography>
    </div>
  );
};

export default Create;
